---
# Default variables for raid_setup role

# Safety flag - set to true to force recreation of existing arrays
force_raid_recreate: false

# Default RAID chunk size (in KB)
raid_chunk_size: 512

# Wait for sync on mirrored/parity arrays
wait_for_sync: true

# Sync wait timeout (in minutes)
sync_timeout: 180
